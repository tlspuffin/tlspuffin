window.SIDEBAR_ITEMS = {"mod":[["mutational","Mutational stage is the normal fuzzing stage. For the current input, it will perform a range of random mutations, and then run them in the executor."]],"struct":[["PushStageHelper","Helper class for the [`PushStage`] trait, taking care of borrowing the shared state"],["PushStageSharedState","Should be stored inside a `[Rc<RefCell<_>>`]"]],"trait":[["PushStage","A push stage is a generator that returns a single testcase for each call. It’s an iterator so we can chain it. After it has finished once, we will call it agan for the next fuzzer round."]]};