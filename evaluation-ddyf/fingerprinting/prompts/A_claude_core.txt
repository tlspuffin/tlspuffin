You are working in the DDYF-fingerprinting repo (a differential TLS protocol fuzzer).
First READ IN FULL: evaluation-ddyf/fingerprinting/LLM_PROMPT.md, PLAN.md and
RESOURCING.md, plus puffin/src/differential.rs and evaluation-ddyf/diff_analyzer.py.

Goal: author the reasoning core of an AI-free fingerprinting method. The delivered
method must contain NO LLM/ML/fuzzer in the classification path (deploy = replay fixed
probe traces, hash TCP responses, walk a static tree.json).

Do this in two stages:

STAGE 1 (now) — Bootstrap against existing data. Implement and debug
evaluation-ddyf/fingerprinting/{triage.py, signatures.py, build_tree.py} using the
objectives already in experiments/. This data is only for exercising code paths; it is
thin/uneven (500vs520 barely ran, ~6 objectives) and 5.1.1 is not built, so DO NOT use
it to claim correctness.

STAGE 2 (after I tell you a fresh validation campaign has finished) — Validate. I will
run, as a separate shell job, short campaigns for all pairs among
{5.0.0, 5.1.0, 5.1.1, 5.2.0}. Validate the pipeline against that output: it MUST
reproduce the appendix result — clusters A0=5.0.0, A1={5.1.0,5.1.1}, B=5.2.0, including
the two named distinguishing traces (TLS1.2 ClientHello session-ticket request
separating 5.2.0; certificate-request -> UnexpectedMessage Alert separating 5.0.0). If
it does not, the filter/canonicalization is wrong — report it, do not tune until it
"looks right".

Honor every Absolute rule in LLM_PROMPT.md, especially:
- the METHOD is AI-free at runtime;
- always work inside `nix-shell ./shell.nix`;
- never fabricate or extrapolate data;
- STOP at each CHECKPOINT, print the requested summary, and wait for me.

Think carefully BEFORE coding about (a) signature canonicalization — strip volatile TLS
fields (randoms, session IDs, timestamps, key shares) so repeated runs match; and (b)
the decision-tree (greedy info-gain) + minimal separating set (greedy set-cover)
algorithms. Do NOT run new fuzzing campaigns yourself (those are shell jobs). Do NOT use
subagents.

After the full 26-version pipeline has been run on the server, I will ask you to
continue with Phase 6 (hardening) and Phase 7 (paper write-up).
