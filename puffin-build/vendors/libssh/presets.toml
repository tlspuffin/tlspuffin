[libssh0104-asan]
sources = { repo = "https://git.libssh.org/projects/libssh.git", branch = "libssh-0.10.4", version = "0.10.4" }
builder = { type = "builtin", name = "libssh" }
asan = true
sancov = true

[libssh0104]
sources = { repo = "https://git.libssh.org/projects/libssh.git", branch = "libssh-0.10.4", version = "0.10.4" }
builder = { type = "builtin", name = "libssh" }
sancov = true

[libssh0114-asan]
sources = { repo = "https://git.libssh.org/projects/libssh.git", branch = "libssh-0.11.4", version = "0.11.4" }
builder = { type = "builtin", name = "libssh" }
asan = true
sancov = true

# gcov-instrumented libssh 0.11.4 for source-level coverage measurement of a
# corpus (no asan/sancov; -O0 + --coverage). Replay a frozen corpus with
# `sshpuffin -T libssh0114-gcov execute <corpus_dir>` to accumulate .gcda.
[libssh0114-gcov]
sources = { repo = "https://git.libssh.org/projects/libssh.git", branch = "libssh-0.11.4", version = "0.11.4" }
builder = { type = "builtin", name = "libssh" }
gcov = true

[libssh0114]
sources = { repo = "https://git.libssh.org/projects/libssh.git", branch = "libssh-0.11.4", version = "0.11.4" }
builder = { type = "builtin", name = "libssh" }
sancov = true


[libssh0803-asan]
sources = { repo = "https://git.libssh.org/projects/libssh.git", branch = "libssh-0.8.3", version = "0.8.3" }
builder = { type = "builtin", name = "libssh" }
asan = true
sancov = true
