name: Run benchmarks

on:
  workflow_dispatch:
  workflow_call:

permissions:
  contents: read

jobs:
  run-benchmarks:
    name: benchmark
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4
      - name: setup
        uses: ./.github/actions/setup
      - name: Benchmark
        shell: bash
        run: just benchmark
