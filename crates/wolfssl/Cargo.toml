[package]
name = "wolfssl"
version = "0.1.0"
edition = "2021"

[features]

[dependencies]
wolfssl-sys = "0.1.7"

libc = { version = "0.2.126" }
foreign-types = { version = "0.5.0" }
bitflags = { version = "1.3.2" }

log = "0.4.17"

[dev-dependencies]

[build-dependencies]
